package enums;

public class StudentStatusPractice {
    public static void main() {
        StudentStatus ssEnum = StudentStatus.ACTIVE;
        if(ssEnum==StudentStatus.ACTIVE)
            System.out.println("Student currently active");

    }
}
